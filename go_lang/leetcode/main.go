package leetcode

func twoSum(nums []int, target int) []int {
	// for i,v := range nums{
	//     // v 를 뺴버려 target에서 그리고target 값이 있으면 ? 끝내 없으면 ? 컨티뉴 ?낫베ㅡㄷ ?

	// }
	return []int{1, 2}
}

func main() {
	// twoSum()
}
